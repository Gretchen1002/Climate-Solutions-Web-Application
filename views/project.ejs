<!DOCTYPE html>
<html lang="en" data-theme="cupcake">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link rel="stylesheet" type="text/css" href="/css/main.css">

        <title>About THIS project</title>
    </head>

    <body>
        <%- include('partials/navbar', {page: ''}) %>
        <br>
        
        <div class="px-4 md:px-8 lg:px-16 xl:px-32">
            <div class="container mx-auto">
                <div class="hero bg-base-200 rounded-lg p-12">
                    <div class="hero-content text-center">
                        <div class="max-w-sm">
                            <h1 class="text-5xl font-bold"><%= project.title %> </h1>
                            <p class="py-6">
                                Below, you will find detailed information about the project: 
                                <em><%= project.title %></em>
                            </p>
                        </div>
                    </div>
                </div>
                <br>

                <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
                    <!--photo-->
                    <div>
                        <img class="w-full rounded-lg" src="<%= project.feature_img_url %>" alt="<%= project.title %>" />
                        <br>
                        <p class="py-6">
                            <%= project.intro_short %>
                        </p>
                    </div>
                    <!--card 2-->
                    <div>
                        <div class="card-body">
                            <h3 class="font-bold">IMPACT:</h3>
                            <p><%= project.impact %> </p>
                            <h3 class="font-bold">FIND MORE INFORMATION:</h3>
                            <p><a href="<%= project.original_source_url %>" target="_blank"><%= project.original_source_url %></a></p>
                            <br>
                            <span>
                                <p id="quote"></p>
                                <p id= "author" style="font-style: italic;"></p>
                            </span>
                        </div>
                    </div>
                    
                </div>

                <div class="text-center">
                    <a href="#" onclick="history.back(); return false" class="btn btn-wide btn-primary">Back to All Projects</a>
                </div>
            </div>
        </div>    
        <br>
        

        <script>
            document.addEventListener("DOMContentLoaded", ()=>{ 
                fetch("https://dummyjson.com/quotes/random" )
                    .then(response =>{
                        return response.json();
                    })
                    .then(quotes=>{
                        document.getElementById('quote').textContent = `"${quotes.quote}"`;
                        document.getElementById('author').textContent = `- ${quotes.author}`;     
                    })
                    .catch(err => console.log("Can't fetch quotes."))               
            });
        </script>
    </body>

    <footer class="text-center font-bold">---End of Page---</footer>
</html>

